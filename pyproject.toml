[project]
name = "microduck-miniverse"
version = "0.1.0"
description = "Reproducible Miniverse bundles for Pollen Robotics Microduck policies."
requires-python = ">=3.10"
dependencies = ["miniverse-sdk>=0.2,<0.3"]

[tool.uv]
package = false
